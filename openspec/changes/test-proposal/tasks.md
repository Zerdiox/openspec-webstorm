# Tasks

## 1. Dry run

- [x] 1.1 Run `openspec validate test-proposal` and confirm the change validates with specs skipped
- [x] 1.2 Run `./gradlew check verifyPlugin` and confirm it stays green (nothing in the plugin changed)

## 2. Manual checks

- [x] 2.1 In the IDE, open the OpenSpec tool window and confirm `test-proposal` is listed as an in-progress change with 0 of its tasks done

## 3. Follow-up harvest

- [x] 3.1 List each out-of-scope issue discovered during implementation as a follow-up candidate in the completion summary; record them (see openspec/backlog/followup/README.md) on Derwa's go.
