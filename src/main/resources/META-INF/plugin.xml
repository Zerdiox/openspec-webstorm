<idea-plugin>
    <id>dev.derwa.openspec</id>
    <name>OpenSpec</name>
    <vendor>Derwa</vendor>
    <description><![CDATA[
    An OpenSpec tool window: lists a project's OpenSpec changes and open follow-ups, and starts
    Claude Code sessions for the OpenSpec workflow steps in IDE terminal tabs.
    ]]></description>

    <!-- The backend module loads where the project lives, the frontend module where the UI is; a
         standalone IDE loads both. -->
    <content>
        <module name="dev.derwa.openspec.shared" loading="required"/>
        <module name="dev.derwa.openspec.backend"/>
        <module name="dev.derwa.openspec.frontend"/>
    </content>
</idea-plugin>
